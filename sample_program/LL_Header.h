/*

	API Call to remove last occurence of a node in Linked List
	
	struct node *lastoccurence(struct node *, int);
	
	Name : lastoccurence
	
	Arguments : 1. struct node * -> takes head node of a linked list
		    
		    2. int -> Node containing number to bre removed (Last Occurenced in a SLL)
		    
	Return : Returns head node of the structure

*/

//API CALL
struct node *lastoccurence(struct node *, int);
